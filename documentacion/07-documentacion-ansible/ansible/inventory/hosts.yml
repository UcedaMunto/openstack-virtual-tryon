# inventory/hosts.yml — Inventario del cluster

all:
  # ─────────────────────────────────────────────────────────────────
  # Variables que aplican a TODOS los nodos
  # ─────────────────────────────────────────────────────────────────
  vars:
    ansible_user: uceda
    ansible_password: asdfghjkl        # sudo password también
    ansible_become_password: asdfghjkl
    ansible_python_interpreter: /usr/bin/python3

  children:
    # ─────────────────────────────────────────────────────────────────
    # Grupo: controller
    # Nodos que alojan los servicios de control de OpenStack:
    # Keystone, Nova API, Neutron Server, Glance, Placement, Horizon,
    # MariaDB, RabbitMQ, Memcached
    # ─────────────────────────────────────────────────────────────────
    controller:
      hosts:
        serverocontroller:
          ansible_host: 203.0.113.239
          mgmt_ip: 10.0.0.11
          public_ip: 203.0.113.239
          # MAC de enp7s0 para fijar en br-mgmt
          mgmt_mac: "52:54:00:99:07:38"

    # ─────────────────────────────────────────────────────────────────
    # Grupo: compute
    # Nodos que ejecutan instancias (nova-compute + neutron-ovs-agent)
    # ─────────────────────────────────────────────────────────────────
    compute:
      hosts:
        compute1:
          ansible_host: 203.0.113.240
          mgmt_ip: 10.0.0.10
          public_ip: 203.0.113.240
          mgmt_mac: "52:54:00:25:40:01"

        compute2:
          ansible_host: 203.0.113.241
          mgmt_ip: 10.0.0.12
          public_ip: 203.0.113.241
          mgmt_mac: "52:54:00:d4:e5:f6"

        compute3:
          ansible_host: 203.0.113.242
          mgmt_ip: 10.0.0.13
          public_ip: 203.0.113.242
          mgmt_mac: "52:54:00:d4:e5:a6"

        compute4:
          ansible_host: 203.0.113.243
          mgmt_ip: 10.0.0.14
          public_ip: 203.0.113.243
          mgmt_mac: "52:54:00:d4:e5:b7"
