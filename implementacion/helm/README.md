# helm/ — Charts Helm de las aplicaciones (futuro)

Aquí irán los charts Helm de los microservicios (frontend, backend,
upload-service, billing-service, tryon-worker) según ARQUITECTURA §63.

Deployment vía Argo CD / GitOps (no `helm install` manual) una vez que las
aplicaciones estén bajo GitOps (ARQUITECTURA §61).
